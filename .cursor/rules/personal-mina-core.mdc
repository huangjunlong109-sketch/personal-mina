---
description: personal-mina 项目级协作规则
alwaysApply: true
---

# personal-mina 规则

- 先确认目标应用：`apps/log-ledger` 或 `apps/log-fuel`，不要默认两边一起改。
- 先确认目标层：`miniprogram` 或 `cloudfunctions`，不要跨层顺手重构。
- 优先最简单方案，不做未被要求的抽象、复用、目录调整或技术栈迁移。
- 只改与需求直接相关的代码；不要顺手清理无关注释、命名、样式或格式。
- 保持现有文件形态：已有 `js` 按 `js` 模式改，已有 `ts` 按 `ts` 模式改；不要为了统一而迁移。
- 遇到同名 `.ts` / `.js` 并存时，先确认哪个是实际维护文件，再修改；不要默认双改。
- 不要跨 `log-ledger` / `log-fuel` 提取公共层，除非明确要求。
- `log-ledger` 数据层基于微信云开发文档型数据库，遵循现有集合设计与字段命名，不按关系型 SQL 思维重构。
- 云函数改动优先保持现有入参、返回结构和集合字段兼容，避免影响已上线数据。
- 完成后做最小验证：相关页面流程、云函数调用路径、最近改动文件的 lint；没有验证条件时明确说明。
- 生成 git 提交信息时默认使用中文，保持简洁，优先说明变更目的而不是罗列文件。
